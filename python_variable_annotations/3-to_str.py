#!/usr/bin/env python3
"""Module for to_str function"""


def to_str(n: float) -> str:
    """a type-annotated function that takes a float n as argument
    and returns the string representation of the float."""
    return str(n)
